{
  "name": "health-visit-blockchain",
  "version": "1.0.0",
  "description": "Smart contracts for health visit verification",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.js --network mumbai",
    "node": "hardhat node",
    "console": "hardhat console"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "ethers": "^5.7.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.17.1",
    "chai": "^4.3.8",
    "mocha": "^10.2.0"
  }
}
