<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .header {
            background-color: #2196F3;
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
        }
        .card {
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .btn-primary {
            background-color: #2196F3;
            border-color: #2196F3;
        }
        .btn-primary:hover {
            background-color: #0d8aee;
            border-color: #0d8aee;
        }
        .success-icon {
            font-size: 4rem;
            color: #4CAF50;
        }
    </style>
</head>
<body>
    <div class="header text-center">
        <div class="container">
            <h1>Health Visit Feedback System</h1>
            <p class="lead">Thank you for your feedback</p>
        </div>
    </div>
    
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card text-center">
                    <div class="card-body">
                        <div class="success-icon">✓</div>
                        <h2 class="card-title mb-3">Feedback Submitted Successfully!</h2>
                        
                        <div class="mb-4">
                            <p class="lead">Your feedback for visit <strong><%= visitId %></strong> has been recorded.</p>
                            <p>Rating: 
                                <% for (let i = 1; i <= rating; i++) { %>
                                    <span class="text-warning">★</span>
                                <% } %>
                                <% for (let i = rating + 1; i <= 5; i++) { %>
                                    <span class="text-muted">★</span>
                                <% } %>
                            </p>
                            
                            <% if (typeof feedbackHash !== 'undefined' && feedbackHash) { %>
                                <div class="alert alert-info">
                                    <strong>Blockchain Hash:</strong> 
                                    <code><%= feedbackHash %></code>
                                    <small class="d-block mt-1">This hash is stored on the blockchain for verification</small>
                                </div>
                            <% } %>
                        </div>
                        
                        <div class="d-grid gap-2">
                            <a href="/" class="btn btn-primary btn-lg">Return to Home</a>
                        </div>
                    </div>
                </div>
                
                <div class="text-center mt-4">
                    <p class="text-muted">Your feedback helps improve healthcare services in your community.</p>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
